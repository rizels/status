<h1>Edit Comment</h1>
<%= form_for [@statusupdate, @comment] do |f| %>
	<%= f.text_area :body, :size => "100x2" %> <br>
	<%= f.submit "Edit Comment", :class => 'btn btn-primary' %>
<% end %>
<%= link_to "Back to status",  statusupdate_path(@statusupdate), :class => 'btn btn-mini' %>
